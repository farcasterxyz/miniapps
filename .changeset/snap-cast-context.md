---
'@farcaster/miniapp-core': minor
---

Add Zod schemas and types for optional cast context on Mini App embed image (Snap) server requests (`castId`, `parentCastId`, `parentHash`, `threadHash`, `embedUrl`). Document the recommended POST body in the Mini App specification.
